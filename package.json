{
  "name": "sayu-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "echo 'Build completed'",
    "start": "cd backend && node sayu-living-server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.0",
    "pm2": "^6.0.6"
  },
  "devDependencies": {
    "@railway/cli": "^4.5.3",
    "conventional-changelog-cli": "^5.0.0"
  }
}
